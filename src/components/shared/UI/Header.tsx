import { UpperHeader } from '../../core/Header/HeaderUpper';
import { BottomHeader } from '../../core/Header/HeaderBottom';

export const Header = () => {
  return (
    <>
      <header>
        <UpperHeader />
        <BottomHeader />
      </header>
    </>
  );
};
