import { UpperHeader } from "../../core/Header/UpperHeader";
import { BottomHeader } from "../../core/Header/BottomHeader";
export const Header = () => {
  return (
    <>
      <header>
        <UpperHeader />
        <BottomHeader />
      </header>
    </>
  );
};
